interface:
  display_name: "Ollama Installing"
  short_description: "Deploy Ollama Compose stacks on servers."
  default_prompt: "Use $ollama-installing to create or harden a production-ready Docker Compose deployment for Ollama on a server, with the official image, manual model pulls, and reverse-proxy-first exposure."
