interface:
  display_name: "Ollama Compose"
  short_description: "Production Ollama Compose stacks"
  default_prompt: "Use $ollama-compose to create or harden a production-ready Docker Compose deployment for Ollama with the official image, env overrides, manual model pulls, and reverse-proxy-first exposure."
