interface:
  display_name: "Traefik Installing"
  short_description: "Install and operate Traefik with a production Compose file"
  default_prompt: "Use $traefik-installing to install or operate Traefik with a production compose file, an HTTP-only dashboard by IP, TLS-ready app routing, and a shared proxy network for app routing."
